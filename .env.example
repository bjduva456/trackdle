SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_REDIRECT_URI=http://127.0.0.1:3000/auth/callback
SESSION_SECRET=change-me-to-a-random-secret

# NOTE: This project uses OAuth PKCE which does NOT require a client secret
# to be stored in the repository or supplied to the frontend. Do NOT commit
# client secrets into version control. Keep any secrets in your local `.env`
# or a secure secret store.